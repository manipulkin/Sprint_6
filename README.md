# Sprint_6 — Автотесты для Яндекс.Самокат

Проект UI-тестирования сервиса https://qa-scooter.praktikum-services.ru/ 

Дла запуска отчета о тестировании в формате веб-страницы необходимо написать в терминале: allure serve allure_results

# Итог исправления всех замечаний:
1) BasePage создала — есть, и наследуются от него

2) Исправила прямые обращения к driver — тепрь работают только через методы BasePage

3) Убрала использование блоков try/except 

4) Исправила зависимость локаторов от позиции в DOM

5) Оформила декораторы @allure.step над каждым методом страниц 

6) URL вынесла в urls.py — хардкода нет, поддерживает переменную окружения

7) Разделила тесты  — логотипы в test_logos_navigation.py, заказ в test_order_flow.py. 

# Итог исправления всех замечаний 2:

1) Замечание: в проекте не должно быть time.sleep
Исправление: метод click_if_clickable_within переписан с использованием WebDriverWait, time.sleep удалён, так же добавила еще декораторы над __init__ и find_elements

2) Замечание:  тестовые данныем лучше вынести их в отдельный модуль (например, data.py) для переиспользования
Исправление: создан файл config/data.py, словари клиентов перенесены туда, а в тесте добавлен импорт from config.data import 

3) Замечание: лучше создать метод в MainPage для получения текущего хоста
Исправление: в MainPage добавлены методы get_base_url() и get_site_host() и тест теперь вызывает их

4) Замечание: лучше добавить проверку точного URL главной страницы
Исправление: в тесте логотипа «Самокат» добавлена проверка assert current_url == base_url

5) Замечание:  в тестах не должно быть условий и блоков try/except
Исправление: параметризованный тест разбит на два независимых теста - test_order_from_top_button (верхняя кнопка) и test_order_from_bottom_button (нижняя кнопка)

6) Замечание: тест ничего не проверял (только ждал модальное окно и закрывал его)
Исправление: в OrderPage добавлен метод wait_for_track_page(), который проверяет переход на страницу трекера заказа (/track). В тестах теперь после закрытия попапа выполняется этот метод и проверка assert "/track" in order.get_current_url()

7) Замечание:  хранение локаторов стоит унифицировать, записать в переменные и хранить в модуле locators или как атрибуты пейджей
Исправление: создан статический метод _metro_option_locator(station_name), который возвращает локатор нужной станции, метод select_metro использует его