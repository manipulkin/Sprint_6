# Sprint_6 — Автотесты для Яндекс.Самокат

Проект UI-тестирования сервиса https://qa-scooter.praktikum-services.ru/ 

Дла запуска отчета о тестировании в формате веб-страницы необходимо написать в терминале: allure serve allure_results

# Итог исправления всех замечаний:
1) BasePage создала — есть, и наследуются от него

2) Исправила прямые обращения к driver — тепрь работают только через методы BasePage

3) Убрала использование блоков try/except 

4) Исправила зависимость локаторов от позиции в DOM

5) Оформила декораторы @allure.step над каждым методом страниц 

6) URL вынесла в urls.py — хардкода нет, поддерживает переменную окружения

7) Разделила тесты  — логотипы в test_logos_navigation.py, заказ в test_order_flow.py. 